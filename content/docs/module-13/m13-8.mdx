---
title: Module 13.8
description: Bcrypt Comparison & Introduction to JWT
---

Installed JWT package and did some of the authentication.

```ts title="./src/modules/auth/auth.routes.ts"
import { Router } from "express";

const router = Router();

router.post("/login"); // [!code highlight]

export const authRoutes = router;
```

```ts title="./src/modules/auth/auth.service.ts"
import bcrypt from "bcryptjs";
import { pool } from "../../config/DB";

const loginUser = async (email: string, password: string) => {
  const result = await pool.query(`SELECT * FROM users WHERE email=$1`, [
    email,
  ]);
  if (result.rows.length === 0) {
    return null;
  }

  const user = result.rows[0];
  const match = await bcrypt.compare(password, user.password);

  if (!match) {
    return null;
  }
};
```
