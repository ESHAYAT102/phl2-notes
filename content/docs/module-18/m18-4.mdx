---
title: "DISTINCT and WHERE Filtering"
description: "Lesson 18.4: DISTINCT and WHERE Filtering."
---

DISTINCT is used when we want unique data. only one value at once.

The WHERE keyword is used to add a condition.

