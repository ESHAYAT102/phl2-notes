---
title: Module 18.5
description: Filtering with AND & OR
---

The OR operator is just like in normal if functions in other programming languages. It's used with WHERE.

The AND operator is also almost the same. It's used with WHERE and can also be used with OR.

