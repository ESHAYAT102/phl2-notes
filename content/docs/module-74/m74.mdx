---
title: Module 74 - 75
description: Go Tickets Project
---

DTO means "data transfer object". We can create seperate objects for requests and responses.

---

You can find the code in [this repo](https://github.com/Apollo-Level2-Web-Dev/gotickets/tree/part-2).

Move the `main.go` file out of the `cmd` folder and delete the empty `cmd` folder,
so now the code can be ran with `go run main.go` instead of needing a longer (full) directory.
