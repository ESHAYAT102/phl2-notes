---
title: Module 22.9
description: Updating and deleting records
---

## Update

Use the `where` keyword to select the section that you want to update, and pass the data in the `data` key.
Must use `update` method.

---

## Delete

Use the `where` keyword for delete too. Must delete all the linked data to delete the value/row.
