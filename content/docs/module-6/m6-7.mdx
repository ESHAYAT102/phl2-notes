---
title: "keyof Constraint with Generics"
description: "Lesson 6.7: keyof Constraint with Generics."
---

```ts
type User = {
  id: string;
  name: string;
  age: number;
};

const getKeyFromObject = <T>(obj: T, key: keyof T) => {
  return obj[key];
};

const me: User = {
  id: "WASD1234",
  name: "Eshayat",
  age: 16,
};

console.log(getKeyFromObject(me, "name"));
```
