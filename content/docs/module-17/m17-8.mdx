---
title: Module 17.8
description: Multiple Constraints & Insert
---

Creating the table:

```
create table users (
  id serial,
  username varchar(50) not null,
  email varchar(100) not null,
  age smallint check (age > 13),
  isActive boolean default false,
  primary key(id),
  unique(username, email)
)
```

Single row insert:

```
INSERT INTO users (id, name, age)
VALUES (0, "Eshayat", 16)
```

Multi row insert:

```
INSERT INTO users (id, name, age)
VALUES
  (1, "Mizan", 29),
  (2, "Rahman", 28),
  (3, "Hasan", 30)
```

Insert without column names:

```
INSERT INTO users
VALUES
  (1, "Mizan", 29),
  (2, "Rahman", 28),
  (3, "Hasan", 30)
```

Insert into specific columns:

```
INSERT INTO users (name, age)
VALUES
  ("Mizan", 29),
  ("Rahman", 28),
  ("Hasan", 30)
```

