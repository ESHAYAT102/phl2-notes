---
title: Module 17.6
description: Create & Drop DB/Table
---

## Basic commands

`create database test` - Create a DB
`drop database test` - Delete a table
`create table test (id serial, name varchar(50))` - Create a table
`drop table test` - Delete a table
`drop table if exists test` - Delete a table if it exists

