---
title: Module 1.7
description: Our Workhorse - The Array
---

# Big O notation

For loops are mostly O(n), as it takes more time for more elements.

These methods are O(1) because they don't iterate through the whole array but simply points to the designated array:

- `push`
- `pop`

If the index is hardcoded then the function will be O(1).
