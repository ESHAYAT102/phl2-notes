---
title: Module 1.10
description: Analysis of O(n) and O(n²)
---

# Overview

Removing duplicates with array with forEach approach is slower that using set (the last code in the previous module).
The array approach is an O(n²) and the set approach is an O(n), that's why it's significantly faster.
