---
title: "Analysis of O(n) and O(n²)"
description: "Lesson 1.10: Analysis of O(n) and O(n²)."
---

# Removing Duplicates Efficiently

A nested array-based approach that scans the accumulated results for every input
value takes O(n²) time in the worst case. Constructing a `Set` visits each value
once on average, so it takes O(n) time and is usually much faster for large inputs.
