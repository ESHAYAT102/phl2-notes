---
title: "Create a Simple Server with Express.js and TypeScript"
description: "Lesson 12.1: Create a Simple Server with Express.js and TypeScript."
---

Created a basic express server.

```ts title="./src/server.ts"
import express, { Request, Response } from "express";
const app = express();
const port = 5000;

app.get("/", (req: Request, res: Response) => {
  res.send("Hello World!");
});

app.listen(port, () => {
  console.log(`Port ${port}`);
});
```

```bash title="file tree"
.
├── bun.lock
├── .env
├── .gitignore
├── package.json
├── src
│   └── server.ts
└── tsconfig.json
```
