---
title: Module 10.11
description: Module Summary
---

What we've leanred in this module:

- reading files with `fs`
- writing files with `fs`
- appending into files with `fs`
- deleting files with `fs`
- joining path with `path`
- using `path` & `fs` together to build a CLI tool
- basics of the `os` module for logging and monitoring
- hashing, encryption, and decryption with `crypto`
